{
  "name": "cryptoteam",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "antd": "^3.1.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-grid-system": "^3.1.2",
    "react-scripts": "1.0.17",
    "react-web3": "^0.4.4",
    "web3": "^0.19.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "production": "npm run build && npm install -g serve && serve -s build"
  }
}